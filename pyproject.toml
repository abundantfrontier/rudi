[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rudi"
version = "0.1.0"
description = "Responsible User-Delegated Interface"
authors = [
    {name = "kb"}
]
dependencies = [
    "pydantic>=2.0.0"
]
